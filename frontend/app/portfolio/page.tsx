"use client";
import { FormEvent, useEffect, useState } from "react";
import { portfolioApi } from "@/services/api/domain";
import { PageHeading } from "@/components/common/page-heading";
import { useLanguage } from "@/i18n/language";

export default function PortfolioPage(){const { text }=useLanguage();const [form,setForm]=useState({grams:0,average_cost:0,planned_investment:0,horizon:"medium",risk_level:"balanced"});const [price,setPrice]=useState(0);const [analysis,setAnalysis]=useState<any>(null);const [status,setStatus]=useState("");useEffect(()=>{portfolioApi.get().then(d=>setForm(f=>({...f,...d}))).catch(()=>setStatus(text("持仓加载失败", "Failed to load portfolio")))},[text]);const save=async(e:FormEvent)=>{e.preventDefault();await portfolioApi.update(form);setStatus(text("已保存", "Saved"));if(price>0)setAnalysis(await portfolioApi.analysis(price))};return <div><PageHeading eyebrow="ADVISORY ONLY" title={text("积存金持仓", "Gold Accumulation Portfolio")} description={text("维护持仓克数、平均成本和计划投入；系统只分析，不连接交易账户。", "Track holdings, average cost, and planned investment. The system analyzes only and never connects to a trading account.")}/><form onSubmit={save} className="card grid gap-4 p-5 sm:grid-cols-2 lg:grid-cols-4">{[[text("持仓克数", "Holdings (g)"),"grams"],[text("平均成本", "Average cost"),"average_cost"],[text("计划投入", "Planned investment"),"planned_investment"]].map(([label,key])=><label key={key} className="text-sm">{label}<input type="number" step="0.01" value={(form as any)[key]} onChange={e=>setForm({...form,[key]:Number(e.target.value)})} className="mt-2 w-full rounded border border-line bg-canvas p-2"/></label>)}<label className="text-sm">{text("当前参考价", "Reference price")}<input type="number" step="0.01" value={price} onChange={e=>setPrice(Number(e.target.value))} className="mt-2 w-full rounded border border-line bg-canvas p-2"/></label><button className="rounded bg-gold p-2 text-black">{text("保存并分析", "Save and analyze")}</button><span className="self-center text-sm text-muted">{status}</span></form>{analysis&&<section className="mt-4 grid gap-3 sm:grid-cols-3">{[[text("市值", "Market value"),analysis.market_value],[text("成本", "Cost basis"),analysis.cost_basis],[text("盈亏", "PnL"),analysis.pnl],[text("收益率", "Return"),`${analysis.pnl_pct.toFixed(2)}%`],[text("盈亏平衡", "Break-even"),analysis.break_even_price],[text("风险级别", "Risk level"),analysis.risk_level]].map(([l,v])=><div key={String(l)} className="card p-4"><div className="text-xs text-muted">{l}</div><div className="tabular mt-2 text-xl">{String(v)}</div></div>)}</section>}</div>}
